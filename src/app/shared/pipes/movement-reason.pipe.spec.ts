import { MovementReasonPipe } from './movement-reason.pipe';

describe('MovementReasonPipe', () => {
  it('create an instance', () => {
    const pipe = new MovementReasonPipe();
    expect(pipe).toBeTruthy();
  });
});
